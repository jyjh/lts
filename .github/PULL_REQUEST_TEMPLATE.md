## Summary

<!-- What does this change do, and why? One or two sentences.
     Link the issue if one exists, e.g. "Closes #12". -->

## How I tested it

<!-- The exact command you ran and what happened, e.g.
     run_audit_tests('ChassisLoadTransferTest.m')  -->

- Command:
- MATLAB release (e.g. R2026a):

## Checklist

- [ ] `run_audit_tests` passes locally (full suite, or the specific test file for my change)
- [ ] No generated files committed (`exports/`, `docs/_site/`, `__pycache__/`, `.pytest_cache/`)
- [ ] Units are SI and stated where values are non-obvious
- [ ] Comments explain *why*, not *what*
- [ ] Any data file over 5 MB was cleared with a maintainer first
- [ ] Title follows `area: short description` — e.g. `suspension: stiffer front ARB for R26`

<!-- If CI is red on this PR: open the failing check, read the first error
     message — it is usually the answer. If not, paste it into the PR
     conversation and ask. Stuck is normal; silent is not. -->
